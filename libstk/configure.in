AC_INIT(AUTHORS)
AM_INIT_AUTOMAKE(libstk, 0.2.0)

AC_PROG_MAKE_SET

dnl AC_PROG_CC
dnl AC_PROG_CPP
AC_PROG_CXX
AC_PROG_INSTALL
AC_PROG_LIBTOOL

dnl AC_LANG_CPLUSPLUS

SDL_VERSION=1.2.0
SIGC_VERSION=1.0.0

AM_PATH_SDL($SDL_VERSION,:,AC_MSG_ERROR([*** SDL version $SDL_VERSION not found!]))
AM_PATH_SIGC($SIGC_VERSION)

AC_SUBST(top_srcdir)
AC_SUBST(SDL_CFLAGS)
AC_SUBST(SDL_LIBS)

AM_CONFIG_HEADER(config.h)

CXXFLAGS="$CXXFLAGS -I $SIGC_CFLAGS"

AC_OUTPUT([
Makefile
src/Makefile
])
